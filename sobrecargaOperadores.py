#Ejemplo de como aplica python la sobrecarga de operadores
a: int = 2
b: int = 3

print(a + b)

#Lista
a = [1, 2, 3, 4, 5]
b = [6, 7, 8, 9, 10]

print(a + b)

#Sobrecargamos el metodo del operador +

"""
    Colecciones: 
        lista: [], ordenadas y mutables
        tupla: (), ordenadas, inmutables
        conjunto: {}, no ordenadas, mutables
        diccionario: {k:v}, no ordenadas, mutables
"""